# Agent Instructions For This Repo

This repo is depth-sensitive. Do not work from memory.

Before substantive claims or edits, especially after compaction/resume/interruption, execute `COMPACTION_PROTOCOL.md`. The short version:

1. Run `git status --short`.
2. Inventory the actual repository tree.
3. Re-read `LOOP_DISCIPLINE.md`, `VISION_COMPLETE.md`, both improvement plans, `GAP_ANALYSIS.md`, `AMBIENT_MODEL.md`, `CHECKLIST.md`, and `HANDOFF.md`.
4. Open the implementation files and tests for every surface you will touch.
5. Build the plan from the current code, not from a previous chat summary.

The newest user instruction wins. The repo beats memory. Tests beat confidence.

Current user mandate to preserve after compaction: Lens is "the" consumer welfare tool, not only a hackathon app. Keep pushing toward a public-release, paper-grade system that protects every point of the customer journey: preference inference before recommendation, browser-extension dark-pattern defense, price comparison, purchase/receipt/recall/subscription monitoring, push notifications, Amazon-to-Temu-style marketplace/counterfeit comparison, optional Plaid-style financial signals, and a fully transparent privacy/encryption/user-control posture visible from the start of the page. Every feature should reduce failure states and move the full welfare architecture forward.
