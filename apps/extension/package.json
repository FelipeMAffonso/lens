{
  "name": "@lens/extension",
  "version": "0.2.0",
  "private": true,
  "description": "Chrome MV3 extension for Lens. Build with `npm run build`, then load-unpacked from dist/.",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.271",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  }
}
